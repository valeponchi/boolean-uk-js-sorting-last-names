const lastNames = ["Mason", "Marcora", "Rico", "Neves", "Ivanov"]
lastNames.push("Ponchietti")
lastNames.sort()

const uppercased = lastNames.map(name => name.toUpperCase());
console.log(uppercased);

